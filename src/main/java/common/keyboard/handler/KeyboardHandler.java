package common.keyboard.handler;

import common.gui.GuiManager;
import common.keyboard.listener.KeyboardListener;
import common.main.ErrorListener;
import common.network.NetworkManager;

public class KeyboardHandler implements KeyboardListener {
    
    public KeyboardHandler(NetworkManager network, GuiManager gui, ErrorListener errorListener){
        
    }
}
